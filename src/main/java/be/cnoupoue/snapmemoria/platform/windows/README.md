# Windows platform placeholder

Windows runtime and packaging support is not implemented yet.

Future work should provide:
- `WindowsPlatformService`
- Windows bundled FFmpeg path resolution
- Windows native folder picker validation
- Windows icon format
- jpackage MSI/EXE workflow
- Windows CI runner workflow
- platform-specific tests
